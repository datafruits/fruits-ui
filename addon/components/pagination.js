import Component from '@glimmer/component';

export default class PaginationComponent extends Component {
  //support @paramName for queryString or default to 'page'
  //support classNames for active page
}
